<Window x:Class="Goliath.aggiungiEsercizio"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Goliath"
        mc:Ignorable="d"
        Title="aggiungiEsercizio" Height="450" Width="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <ListBox x:Name="VideosListBox"
                 d:ItemsSource="{d:SampleData ItemCount=5}"
                 HorizontalContentAlignment="Stretch"
                 SelectionChanged="VideosListBox_SelectionChanged"
                 Grid.RowSpan="2"/>

        <MediaElement x:Name="videoPlayer"
                      Grid.Column="1"
                      Margin="75,34,51,97"
                      UnloadedBehavior="Manual" Grid.Row="1"/>

        <Button x:Name="buttonReturn" Content="RETURN"
                Grid.Column="1"
                Grid.Row="1"
                HorizontalAlignment="Right"
                VerticalAlignment="Top"
                Margin="0,308,32,0"
                Width="130"
                Height="54" Click="buttonReturn_Click"/>
    </Grid>
</Window>
