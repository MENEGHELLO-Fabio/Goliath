<Window x:Class="Goliath.aggiungiEsercizio"
        ResizeMode="NoResize" 
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Goliath"
        mc:Ignorable="d"
        Title="aggiungiEsercizio" Height="450" Width="800" Background="#FF393939">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <ListBox x:Name="VideosListBox"
                 d:ItemsSource="{d:SampleData ItemCount=5}"
                 HorizontalContentAlignment="Stretch"
                 SelectionChanged="VideosListBox_SelectionChanged"
                 Grid.RowSpan="2"/>

        <MediaElement x:Name="videoPlayer"
                      Grid.Column="1"
                      Margin="75,34,51,97"
                      UnloadedBehavior="Manual" Grid.Row="1"/>

        <Button x:Name="buttonReturn" Margin="384,310,50,30" Grid.Row="1" Grid.Column="1" Content="Salva" FontFamily="Verdana" Click="buttonReturn_Click_1">
            <Button.Resources>
                <Style TargetType="{x:Type Border}">
                    <Setter Property="CornerRadius" Value="12"/>
                </Style>
            </Button.Resources>
            <Button.Background>
                <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                    <GradientStop Color="#FEA36AD7"/>
                    <GradientStop Color="#FF9A9A9A" Offset="1"/>
                </LinearGradientBrush>
            </Button.Background>
        </Button>
    </Grid>
</Window>
